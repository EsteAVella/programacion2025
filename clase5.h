#include <stdlib.h>

void* encriptarChar( int TAM, int offset, char* tipo, char* data);

void* encriptar( size_t, int offset, char* tipo, char* data);

//int cmp(int a, int b);

int asc(int a, int b);

int des(int a, int b);

int ascF(float a, float b);

int desF(float a, float b);
